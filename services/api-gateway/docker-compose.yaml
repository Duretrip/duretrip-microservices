version: '3.7'

services:
  main:
    container_name: auth
    build:
      context: .
      dockerfile: Dockerfile
    image: duretrip/api-gateway:latest
    restart: always
    ports:
      - 8006:8006
    # env_file:
    #   - .env.dev